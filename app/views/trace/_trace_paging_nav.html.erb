<p>
<%= link_to t('trace.trace_paging_nav.newer'), params.merge({ :page => @page - 1 }) if @page > 1 %>
<% if @traces.size > 1 and @page > 1 %>
|
<% end %>
<%= link_to t('trace.trace_paging_nav.older'), params.merge({ :page => @page + 1 }) if @traces.size >= @page_size %>
</p>
